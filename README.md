# React TODO アプリ

高機能な TODO アプリケーションです。React と Vite を使用して構築され、ローカルストレージでデータを永続化します。

## 機能

- ✅ **CRUD 操作**: タスクの作成、読み取り、更新、削除
- ✅ **完了/未完了の切り替え**: チェックボックスでタスクの状態を管理
- ✅ **カテゴリ機能**: タスクをカテゴリで分類
- ✅ **タグ機能**: 複数のタグでタスクを整理
- ✅ **期限設定**: タスクに期限を設定し、期限切れタスクを赤字で表示
- ✅ **フィルタリング**: ステータス、カテゴリ、タグでタスクをフィルタリング
- ✅ **ソート機能**: 作成日時、期限、タイトルで並び替え
- ✅ **レスポンシブデザイン**: デスクトップ、タブレット、モバイルに対応
- ✅ **データ永続化**: ローカルストレージでデータを保存

## 必要な環境

- Node.js (v16 以上推奨)
- npm または yarn

## インストール

1. Node.js をインストールしていない場合は、[Node.js 公式サイト](https://nodejs.org/) からダウンロードしてインストールしてください。

2. プロジェクトのディレクトリで以下のコマンドを実行します。

```bash
npm install
```

## 起動方法

開発サーバーを起動します。

```bash
npm run dev
```

ブラウザで `http://localhost:5173` を開いてアプリケーションにアクセスします。

## ビルド

本番用にビルドする場合は以下のコマンドを実行します。

```bash
npm run build
```

ビルドされたファイルは `dist` フォルダに出力されます。

## プロジェクト構造

```
todo/
├── index.html              # エントリーポイント HTML
├── package.json            # プロジェクト設定と依存関係
├── vite.config.js          # Vite 設定
├── src/
│   ├── main.jsx           # React アプリのエントリーポイント
│   ├── App.jsx            # メインアプリコンポーネント
│   ├── App.css            # アプリ全体のスタイル
│   ├── components/
│   │   ├── TodoForm.jsx   # タスク追加/編集フォーム
│   │   ├── TodoList.jsx   # タスクリスト表示
│   │   ├── TodoItem.jsx   # 個別タスクアイテム
│   │   └── Filter.jsx     # フィルタリングコンポーネント
│   ├── hooks/
│   │   └── useTodos.js    # カスタムフック（状態管理）
│   └── utils/
│       └── storage.js     # ローカルストレージユーティリティ
└── .gitignore             # Git 除外設定
```

## 使い方

### タスクの追加

1. フォームにタスク情報を入力します
   - タイトル（必須）
   - 説明（オプション）
   - カテゴリ（オプション）
   - タグ（カンマ区切りで複数指定可能）
   - 期限（オプション）

2. 「追加」ボタンをクリックします

### タスクの完了/未完了の切り替え

タスクの左側にあるチェックボックスをクリックします。

### タスクの編集

1. タスクの「編集」ボタンをクリックします
2. フォームが編集モードになり、既存の情報が表示されます
3. 情報を変更して「更新」ボタンをクリックします

### タスクの削除

タスクの「削除」ボタンをクリックします。

### フィルタリングとソート

右側のサイドバーで以下の操作ができます:

- **ステータスフィルター**: 全て / 未完了 / 完了
- **カテゴリフィルター**: 特定のカテゴリのみ表示
- **タグフィルター**: 特定のタグを含むタスクのみ表示
- **並び替え**: 作成日時、期限、タイトルで並び替え

## データの永続化

すべてのタスクはブラウザのローカルストレージに保存されます。ページをリロードしてもデータは保持されます。

## 技術スタック

- **React 18**: UI ライブラリ
- **Vite**: 高速なビルドツール
- **CSS**: カスタムスタイリング
- **LocalStorage API**: データ永続化

## ライセンス

MIT
